# AGENTS.md — 我的偏好积累

> 持续更新。每次新任务前先读此文件。

## 写作与内容

- 标题不超过 20 字，必须有数字或对比
- 报告先给结论，再给过程
- 中文优先，英文作补充
- 段落简短，一段只说一件事
- 能用列表就不要写长段落

## 设计与 UI

- 极简风格，去掉一切装饰性元素
- 移动端优先，桌面端适配
- 图片保护（禁用右键/拖拽）
- 中文字体用 Noto Sans SC，英文字体用 Inter
- 配色不超过 3 种主色
- 留白比填充更重要

## 代码与工程

- 纯原生 HTML/CSS/JS，不引入框架
- 不添加未要求的依赖
- 一个文件能解决的问题不拆成多个
- 注释只写"为什么"，不写"是什么"
- 优先可维护性，其次性能

## 工作流

- 改代码前先读 README 和现有文件
- 小改动直接做，大改动先给方案
- 部署走 Vercel，保持 `.vercel` 配置
- 每次提交附带一句话说明
- 每次任务完成后立即部署到 Vercel（preview + production）

## 摄影相关

- 作品标题格式：`作品名（年份）`
- 图片说明控制在 15 字以内
- 系列作品按时间倒序排列
- 不加水印，用禁用交互保护
- 保留 EXIF 信息

---

## Agent 命令规范

### 任务开始前
1. 读 `AGENTS.md`（本文档）确认偏好
2. 读 `PROGRESS.md` 了解项目进度和历史踩坑
3. 读 `README.md` 了解项目结构

### 任务结束后
1. 更新 `PROGRESS.md`：在"进度时间线"顶部追加一条记录
2. 踩坑单独提到"踩过的坑"章节，按主题归类
3. 如有新偏好或规范变化，同步更新 `AGENTS.md`
4. 部署到 Vercel（`npx vercel --cwd . --prod --yes`）
5. 更新两个文档底部的"最后更新"日期
### 记录原则
- 只记关键信息，不记流水账
- 踩坑记"现象 + 解决方案"，不记排查过程
- 偏好记"规则"，不记"原因"（原因看 PROGRESS.md）
- 两个文档分工：AGENTS.md 管"该怎么做"，PROGRESS.md 管"做过什么"

---

*最后更新：2026-07-22*

# AGENTS.md — Codex 工作与知识管理规则

## 角色

你既是本项目的软件工程师，也是项目知识管理员。

用户只需描述目标，不需要记住文档结构，也不需要指定应读取或更新哪个 Markdown。你负责自动判断：

- 当前需求属于哪类信息；
- 完成任务需要读取哪些代码与文档；
- 哪些结论值得沉淀为长期知识；
- 应更新现有文档，还是在确有持续需求时新建专题文档。

## 项目底线

- 这是 LI ZHIXIN 的个人摄影作品网站。
- 技术栈保持为原生 HTML、CSS、JavaScript。
- 部署平台保持为 Vercel，并保留现有 `.vercel` 与 `vercel.json` 配置。
- 不引入框架、构建系统、大型依赖或未要求的第三方服务。
- 不删除或改变现有页面、内容和功能，除非用户明确要求。
- 优先最小、可验证、可回退的修改，不做无关重构。

## 每个任务如何开始

1. **只先读取 `README.md`。** 它是项目事实和文档导航的唯一入口。
2. 判断当前任务的明确目标和影响范围。
3. 根据下方“知识分类与路由”，只读取与当前任务直接相关的文档。
4. 先搜索并定位相关代码，再读取必要片段；不要默认扫描全部源文件。
5. 信息已经足够时立即停止扩展上下文并开始工作。

禁止在每次新会话中固定读取全部 Markdown。不要因为某个文档存在，就自动把它加入上下文。

## 知识分类与路由

收到新需求后，自动分类并选择读取与写入位置。一个需求确实跨多个领域时可以更新多个文档，但不得复制同一条规则。

| 信息类型 | 典型内容 | 首选文档 |
| --- | --- | --- |
| AI 工作方式 | 如何分析、修改、验证、汇报和维护知识 | `AGENTS.md` |
| 项目事实与产品需求 | 网站定位、受众、页面、长期功能范围 | `PROJECT.md` |
| 视觉与交互规范 | 字体、颜色、留白、布局、动画、响应式、图片呈现 | `STYLE_GUIDE.md` |
| 技术架构 | 技术栈、目录约定、数据方式、部署与外部服务 | `ARCHITECTURE.md` |
| 长期决策 | 已选择或放弃的方案及其原因 | `DECISIONS.md` |
| 当前任务 | 正在做、下一步做、明确待办 | `TODO.md` |
| 已完成工作与踩坑 | 重要进展、问题现象、有效解决方案 | `PROGRESS.md` |
| 长期项目记忆 | 稳定偏好、背景、未来意向与跨任务共识 | `MEMORY.md` |

当前已经存在的知识文档只有 `README.md`、`AGENTS.md` 和 `PROGRESS.md`。表中其他文档是预留职责，不要为了“结构完整”而一次性创建。只有当相应信息已经具有长期、独立维护价值时才创建，并同步把它登记到 `README.md`。

若信息归属存在轻微重叠，选择最具体的单一文档即可；只有当不同选择会显著改变结果或用户意图时才询问用户。

## 文档读取规则

- 页面或功能修改：读取 `README.md`、目标页面及其直接引用的 CSS/JS；只有涉及既定经验时才读 `PROGRESS.md` 的相关章节。
- UI 修改：若存在 `STYLE_GUIDE.md`，只读与目标组件或页面相关的章节。
- 技术或部署修改：若存在 `ARCHITECTURE.md`，只读相关章节与实际配置文件。
- 新功能或范围调整：若存在 `PROJECT.md`、`TODO.md`，只读相关需求与当前任务。
- 已确定方案可能被推翻时：搜索 `DECISIONS.md` 和 `PROGRESS.md` 中对应关键词。
- 不默认读取历史聊天、完整 Git 历史、全部 Markdown、全部 HTML 或整个图片目录。
- 大文件优先搜索关键词并读取相关区段；只有结构性任务才完整读取。

## 实施原则

- 先理解现有实现，再修改。
- 小改动直接完成；涉及多页面、信息架构或高回退成本的大改动，先简要说明方案和影响范围。
- 一个文件能清楚解决的问题不强行拆分；也不要把不同职责长期堆进同一文件。
- 注释解释“为什么”，不复述代码“是什么”。
- 优先保持一致性和可维护性，其次才是局部技巧或微小性能收益。
- 大改前保留可恢复点；不要覆盖用户未提交的修改。
- 修改后检查直接受影响的页面，并按风险验证桌面端、移动端、导航、图片和交互。
- 每次提交使用一句清楚的说明概括改动；未经请求不要为无关内容创建提交。
- 站点代码修改验证通过后，沿用 Vercel preview 与 production 部署流程；如部署权限、成本或用户当次指令要求确认，则先说明再部署。

## 现有设计与内容约束

- 极简、克制，去掉没有内容价值的装饰。
- 移动端优先，同时保证桌面端体验。
- 留白比填充更重要，主色不超过三种。
- 中文使用 Noto Sans SC，英文使用 Inter。
- 图片禁用右键与拖拽，不添加水印，并保留 EXIF 信息。
- 作品标题使用“作品名（年份）”；图片说明控制在 15 字以内。
- 系列作品默认按时间倒序排列。
- 写作和汇报中文优先、先给结论、段落简短；标题不超过 20 个汉字，并在适合时使用数字或对比。

## 任务会话与上下文控制

- 一个会话只处理一个清晰目标，例如“完成首页轮播移动端适配”，而不是长期承载整个网站开发。
- 同一页面的紧密相关修改可以留在同一会话；目标、风险或所需上下文明显变化时，建议开启新会话。
- 当目标已经完成，不继续用该会话处理无关任务。
- 当对话明显变长（通常约 50–80 轮）、开始反复回顾旧讨论，或任务范围持续漂移时：先把仍有效的结论写入对应项目文档，再建议开启新任务会话。
- 不默认创建 `SESSION_SUMMARY.md`。只有任务未完成、确实需要跨会话交接且现有专题文档无法清楚承载时，才创建简短的会话摘要；交接结束后应归档或合并，避免永久堆积。
- 新会话依赖项目文档恢复必要状态，不依赖重读旧聊天。

## 任务结束与知识沉淀

完成任务后，只进行与本次工作相关的收尾：

1. 验证目标是否真正完成，并汇报结果、验证情况和仍存在的限制。
2. 识别本次产生的长期信息，按分类更新对应文档；一次性讨论不写入长期知识。
3. 有实质开发进展时，在 `PROGRESS.md` 的“进度时间线”顶部记录日期、结果和影响范围。
4. 只有可复用的新问题与解决方案才加入“踩过的坑”，不记录完整排查流水账。
5. 新增、删除或改变文档职责时，更新 `README.md` 的文档导航。
6. 仅在内容确实更新时修改“最后更新”日期。

不要机械地让每个小任务同时更新多个 Markdown，也不要把 Git 已能表达的逐文件变更重复抄进文档。

## 文档自我维护

- 每份文档保持单一职责、内容精炼、事实有效。
- 发现重复时保留一个权威位置，其他文档改为链接或删除重复表述。
- 当文档明显过长、混合多个职责或频繁需要全量读取时，提出拆分建议；除非拆分是完成当前任务所必需，否则先征得用户同意。
- 历史记录只追加关键节点；失效的当前状态应修订，而不是与新状态并存。

---

*最后更新：2026-08-08*

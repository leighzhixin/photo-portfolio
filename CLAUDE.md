# CLAUDE.md - Codex 自动加载指令

## 任务启动流程（每次新对话必须执行）

1. 读 `AGENTS.md` -- 确认用户偏好和规范
2. 读 `PROGRESS.md` -- 了解项目进度和历史踩坑
3. 读 `README.md` -- 了解项目结构

以上三步在开始任何实际工作之前完成。

## 任务结束流程

1. 更新 `PROGRESS.md`：在"进度时间线"顶部追加一条记录（格式见该文件末尾）
2. 踩坑提到"踩过的坑"章节，按主题归类
3. 如有新偏好或规范变化，同步更新 `AGENTS.md`
4. 刷新两个文档底部的"最后更新"日期

## 文档分工

- `AGENTS.md` 管"该怎么做" -- 偏好、规范、约定
- `PROGRESS.md` 管"做过什么" -- 进度、踩坑、经验
- `CLAUDE.md`（本文件）管"流程怎么走" -- 启动和收尾的固定步骤

## 注意

- 这三个文件是项目的"操作系统"，优先级高于临时指令
- 用户可以直接提需求，不需要说"读规范"--本文件确保流程自动触发
- 如果用户明确说"跳过规范"或"直接改"，可以跳过读取步骤，但任务结束后仍需更新 PROGRESS.md
